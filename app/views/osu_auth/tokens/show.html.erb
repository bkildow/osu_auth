<ul class="nav nav-tabs">
  <li role="presentation"><%= link_to 'Edit User', edit_user_path(@user) %></li>
  <li role="presentation" class="active"><a href="#">API Token</a></li>
</ul>

<fieldset>
  <legend>Token</legend>
  <%= @user.auth_token %>
</fieldset>

<%= button_to 'Generate', token_path(@user.id), method: :patch %>
<%= button_to 'Delete', token_path(@user.id), method: :delete %>
